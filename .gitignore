# Never commit any Messages/AddressBook data
*.db
*.db-wal
*.db-shm
chat.db*
AddressBook*

# Python / macOS cruft
__pycache__/
*.pyc
.DS_Store

# Never commit secrets (Bearer token, approve secret, ntfy topic, tunnel creds)
*.env
.env
secrets*
*.pem
